# 2026-04-28 — Setup day

First working day in the Growth Engine repo. Goal: from zero to a working Git flow + a place for notes.

## What's done

- Git install verified (already present)
- GitHub CLI (`gh`) installed via `winget`
- Logged in to GitHub as `robbinvanhassel` with a private noreply email
- Repo `levihendrickx/growthengine` cloned to `C:\Users\Maurice\Desktop\Claude Code\growthengine`
- First commit created and pushed (`Add NOTES.md — first commit`)
- Merged with Levi's parallel work (no conflicts)
- Collaborator permission confirmed: `write`
- `docs/` folder set up

## Lessons

- Always `git pull` before you start, `git push` at the end. Got lucky today because we were working on different files.
- A merge commit appears automatically when your local commit and remote diverge. Not a problem, just visible in the history.

## Next step

- Get familiar with the existing code in `webapp/` and `scripts/parse_meta_csv.py` to understand where Levi stands.
- Plan POC work: Stoney data → local Atomspace.
