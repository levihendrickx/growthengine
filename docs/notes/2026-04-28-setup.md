# 2026-04-28 — Setup dag

Eerste werkdag in de Growth Engine repo. Doel: van nul naar werkende Git-flow + plek voor notities.

## Wat is er gedaan

- Git-installatie geverifieerd (al aanwezig)
- GitHub CLI (`gh`) geïnstalleerd via `winget`
- Ingelogd op GitHub als `robbinvanhassel` met privé-email (noreply)
- Repo `levihendrickx/growthengine` geclonet naar `C:\Users\Maurice\Desktop\Claude Code\growthengine`
- Eerste commit gemaakt en gepusht (`Voeg NOTES.md toe — eerste commit`)
- Gemerged met Levi's parallel werk (geen conflicten)
- Collaborator-rechten bevestigd: `write`
- `docs/` map opgezet als Obsidian-vault

## Lessen

- Altijd `git pull` voor je begint, `git push` aan het eind. Vandaag had ik geluk dat we aan andere bestanden werkten.
- Een merge-commit ontstaat automatisch als je commit + remote uit elkaar lopen. Niet erg, wel zichtbaar in de history.

## Volgende stap

- Obsidian openen, vault `docs/` aanwijzen, even rondklikken.
- Daarna: kennismaking met de bestaande code in `webapp/` en `scripts/parse_meta_csv.py` om te snappen waar Levi staat.
- POC-werk plannen: Stoney data → lokale Atomspace.
