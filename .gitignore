node_modules/
.env
.env.local
.claude/settings.local.json

# Obsidian — vault config & cache (notes themselves are tracked, settings are personal)
docs/.obsidian/

# Atomspace data — generated atoms are runtime data, not source code
# (also contains Stoney's metrics — sensitive). Everyone runs scripts/import_*.py again.
atoms/
