node_modules/
.env
.env.local
.claude/settings.local.json

# Obsidian — vault config & cache (notes themselves are tracked, settings are personal)
docs/.obsidian/

# Atomspace data — gegenereerde atoms zijn runtime-data, niet broncode
# (bevat ook Stoney's metrics — gevoelig). Iedereen draait scripts/import_*.py opnieuw.
atoms/
