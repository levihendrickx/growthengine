node_modules/
.env
.env.local
.claude/settings.local.json

# Obsidian — vault config & cache (notes themselves are tracked, settings are personal)
docs/.obsidian/

# Atomspace data — generated atoms are runtime data, not source code
# (also contains Stoney's metrics — sensitive). Everyone runs scripts/import_*.py again.
# Exception: sample_data/atoms/ is a committed snapshot for backend development.
atoms/
!sample_data/atoms/
!sample_data/atoms/**

# Downloaded ad images — runtime output from import_meta_creative.py.
# Same reasoning as atoms/: regenerate from the API, do not commit.
images/
# Exception: webapp/images/ holds static UI assets (hero photos etc.)
!webapp/images/
!webapp/images/**
# Exception: sample_data/images/ is a curated subset of real Stoney ad images
# committed alongside sample_data/atoms/ so backend/PLN/demo dev can render
# the actual creatives without needing Meta credentials.
!sample_data/images/
!sample_data/images/**

test_exec.mjs
